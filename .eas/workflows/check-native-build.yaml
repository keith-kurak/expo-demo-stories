name: Check native build exists

on:
  push:
    branches:
      - "*.*.0"

jobs:
  fingerprint:
    name: Fingerprint
    type: fingerprint
    env:
      APP_VERSION: ${{ github.ref_name }}

  get_android_build:
    name: Find Android build
    needs: [fingerprint]
    type: get-build
    params:
      fingerprint_hash: ${{ needs.fingerprint.outputs.android_fingerprint_hash }}
      runtime_version: ${{ github.ref_name }}
      profile: production

  get_ios_build:
    name: Find iOS build
    needs: [fingerprint]
    type: get-build
    params:
      fingerprint_hash: ${{ needs.fingerprint.outputs.ios_fingerprint_hash }}
      runtime_version: ${{ github.ref_name }}
      profile: production-simulator # for demonstration purposes only

  verify_builds:
    name: Verify builds exist
    needs: [get_android_build, get_ios_build]
    steps:
      - name: Check Android build
        run: |
          if [ -z "${{ needs.get_android_build.outputs.build_id }}" ]; then
            echo "No Android production build found matching fingerprint and runtime version ${{ github.ref_name }}"
            exit 1
          fi
          echo "Android build found: ${{ needs.get_android_build.outputs.build_id }}"
      - name: Check iOS build
        run: |
          if [ -z "${{ needs.get_ios_build.outputs.build_id }}" ]; then
            echo "No iOS production build found matching fingerprint and runtime version ${{ github.ref_name }}"
            exit 1
          fi
          echo "iOS build found: ${{ needs.get_ios_build.outputs.build_id }}"
